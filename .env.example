# Copy to .env.local for local dev, or set as a Vercel project environment
# variable. Used only by /api/eval (the Decision Eval Tool). The chapter
# content is static and needs no key.
ANTHROPIC_API_KEY=
